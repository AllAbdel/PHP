{% extends 'base.html.twig' %}

{% block body %}
    <h2>Créer un produit</h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
